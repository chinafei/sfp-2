# SFP-2 Docker 镜像依赖
# 与 llm-on-ray 环境保持兼容

# PyTorch CPU（Docker 中不使用 GPU）
torch==2.6.0
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
scikit-learn==1.6.1

# API 服务
fastapi==0.115.12
uvicorn[standard]==0.34.0
pydantic==2.12.5

# 调参
optuna==4.8.0

# Excel 读取（数据处理说明.xlsx）
openpyxl==3.1.5
